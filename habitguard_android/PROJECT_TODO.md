# HabitGuard TODO

* [ ] High / Add remaining Android tests for goal parsing, habit scoring, mission UI validation, Firestore payload creation, and device-run Compose flows / Usage aggregation, Guard v2 evaluator, prediction repository, dashboard presenter, Room 10-to-11 migration, and basic Compose Semantics now have coverage, but these areas remain incomplete / `app/src/test`, `app/src/androidTest`
* [ ] High / Fix the existing Compose instrumentation launch failure in `ScreenTimingComposeSemanticsTest` / Full `connectedDebugAndroidTest` fails with "No compose hierarchies found"; migration tests pass when filtered / `app/src/androidTest/java/com/habitguard/app/ui/ScreenTimingComposeSemanticsTest.kt`
* [ ] High / Complete real-device Guard v2 testing after re-enabling Usage Access, AccessibilityService, and NotificationListener on the reinstalled device / Device run found and fixed a Room 7-to-11 launch crash, but restriction/mission scenarios were not completed / `docs/android_device_test_results.md`
* [ ] High / Run and record real-device results for usage access, accessibility lock flow, mission success/abandon, notification counting, and CSV export / Only partial Usage Access allow/revoke evidence exists; Guard and CSV flows still need sanitized device evidence / `docs/android_device_test_results.md`
* [ ] High / Run Guard v2 real-device tests on connected tablet for 23:00-06:00 rules, temporary unlock expiry, reboot invalidation, home/recent/back bypass, emergency unlock, and same-app re-entry / JVM tests pass but AccessibilityService and LockActivity flows are not device-verified / `docs/android_device_test_plan.md`, `app/src/main/java/com/habitguard/app/guard/`
* [ ] High / Validate the new UsageStats event aggregation on the connected running tablet with permission grant, permission revoke, reboot, and date-boundary scenarios / JVM tests pass, but OEM UsageStats event retention and tablet behavior are not yet recorded / `docs/android_device_test_plan.md`, `app/src/main/java/com/habitguard/app/data/UsageStatsRepository.kt`
* [ ] High / Run `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest` on a connected Samsung/Pixel device and record the result / androidTest APK builds, but no device was listed by `adb devices` during the quality verification task / `app/src/androidTest`, `TEST_FAILURES.md`
* [ ] High / Add production cloud auth/config UI or hide Firestore sync controls until `SecureTokenStore` can be configured safely / Secure token read path exists but no user-safe setup flow exists / `FirestoreSyncRepository.kt`, `HabitGuardApp.kt`
* [ ] High / Keep prediction backend work out of the app until auth, user authorization, rate limit, and environment separation are designed / App now has an offline WorkManager + local JSON-bundle prediction path; no production backend is configured or needed for current local-first prediction / `app/src/main/java/com/habitguard/app/ai/PredictionRepository.kt`, `app/src/main/java/com/habitguard/app/data/DailyUsageSyncWorker.kt`
* [ ] Medium / Extend local data deletion to clear SharedPreferences and encrypted cloud token settings when the user requests a full privacy reset / Current reset clears Room tables only / `MainActivity.kt`, `GuardPreferences.kt`, `SecureTokenStore.kt`
* [ ] Medium / Add rule management UI for viewing, disabling, editing, and deleting rules / Current UI creates an active rule but does not fully manage multiple rules / `HabitGuardApp.kt`, `MainActivity.kt`
* [ ] Medium / Run Screen Timing UI QA at 200% font size, landscape, dark mode, and manual TalkBack reading / Current UI was screenshot-tested on a connected tablet and uiautomator dumps were saved, but these accessibility display modes were not fully verified / `docs/screen_timing_design/`, `app/src/main/java/com/habitguard/app/ui/`
* [ ] Medium / Clean up Korean recommendation strings in `GoalParser` and add parser copy tests / Screen Timing UI copy was cleaned up, but parser-generated recommendation reasons still need a separate copy-quality pass / `app/src/main/java/com/habitguard/app/ai/GoalParser.kt`, `app/src/test`
* [ ] Medium / Train and validate the completed ML pipeline with an approved real HabitGuard CSV export and label outputs as real only after verification / Current Android local inference bundle is generated from synthetic data only, even though actual Room usage summaries can now feed offline inference / `ai/train_from_phone_csv.py`, `ai/phone_outputs/`, `app/src/main/assets/android_inference_bundle.json`
* [ ] Medium / Real-device QA and screenshot the AI prediction card on the connected tablet / JVM tests cover local inference math and fallback behavior, but Compose rendering still needs a fresh device capture / `HabitGuardApp.kt`, `docs/android_device_test_plan.md`
* [ ] Low / Add release signing and store review notes before distribution / No release signing or Play review evidence found / `docs/`, Gradle config
